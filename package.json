{
  "name": "junior2senior",
  "version": "1.0.0",
  "description": "",
  "main": "index.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1",
    "prettier": "./node_modules/.bin/prettier --write './**/*.{ts,js,css,json}'",
    "precommit": "lint-staged"
  },
  "lint-staged": {
    "*.{ts,js,css,son}": [
      "./node_modules/.bin/prettier --write",
      "git add"
    ]
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {},
  "devDependencies": {
    "husky": "^4.2.5",
    "lint-staged": "^10.1.7",
    "prettier": "^2.0.5"
  }
}
